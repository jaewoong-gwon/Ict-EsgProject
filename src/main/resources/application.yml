server:
  port: 81
  servlet:
    encoding:
      charset: UTF-8

mybatis:
  configuration:
    map-underscore-to-camel-case : true

spring:
  #datasource ?? - mybatis-config.xml ???? ??.
  datasource:
    driver-class-name : com.mysql.cj.jdbc.Driver
    url : jdbc:mysql://localhost:3306/esg?characterEncoding=utf8
    username : esg_user
    password : esg

  logging:
    level:
      Ict.esgProject : DEBUG


